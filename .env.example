# FusionSolar Login Credentials
FUSIONSOLAR_USERNAME=your_username_or_email
FUSIONSOLAR_PASSWORD=your_password

# Optional: Polling interval in minutes (default: 1)
POLL_INTERVAL_MINUTES=1

# Optional: Set to false to see browser during login (for debugging)
# HEADLESS=true

# Optional: Set to true to log full JSON response (for debugging)
# DEBUG=false

# MQTT Configuration (optional - for Home Assistant integration)
# MQTT_HOST=localhost
# MQTT_PORT=1883
# MQTT_USERNAME=
# MQTT_PASSWORD=

# Station DN (device identifier)
# STATION_DN=NE=169590965
